# איך מעלים את זה לגיטהאב — צעד אחרי צעד

בסוף התהליך יהיה לך: עמוד אינטרנט עם קישורי הורדה לתוכנה, וגרסה חיה
שרצה אונליין ונפתחת מכל מקום — גם מהטלפון. הכל בחינם.

**זמן משוער: 15 דקות. לא צריך לדעת שום דבר טכני.**

---

## חלק א' — העלאה לגיטהאב (עמוד ההורדה)

### 1. פתח חשבון גיטהאב (אם אין לך)

היכנס ל-**github.com**, לחץ **Sign up**, ועקוב אחרי ההרשמה הרגילה
(מייל, סיסמה, אימות).

### 2. צור מאגר חדש

1. לחץ על **+** בפינה הימנית העליונה ← **New repository**.
2. בשדה **Repository name** כתוב: `telegram-popup`
3. השאר **Public** מסומן (כדי שאפשר יהיה להוריד בלי חשבון).
4. לחץ על הכפתור הירוק **Create repository**.

### 3. העלה את הקבצים

1. חלץ את `TelegramPopup-GitHub.zip` לתיקייה במחשב.
2. בעמוד המאגר החדש, לחץ על הקישור **uploading an existing file**
   (באמצע העמוד).
3. פתח את התיקייה שחילצת, סמן את **כל** מה שבפנים (Ctrl+A),
   וגרור לתוך החלון של גיטהאב.
4. המתן שהכל יעלה (יש גם תיקיות — גיטהאב שומר עליהן).
5. למטה לחץ על הכפתור הירוק **Commit changes**.

### 4. זהו — עמוד ההורדה חי

היכנס לעמוד המאגר: `github.com/שם-המשתמש-שלך/telegram-popup`
תראה את העמוד המעוצב עם קישורי ההורדה. כל מי שתשלח לו את הקישור
יוכל להוריד את התוכנה — בלי חשבון גיטהאב.

---

## חלק ב' — הגרסה האונליין (5 דקות נוספות)

### 1. הירשם ל-Render

היכנס ל-**render.com** ← **Get Started** ← בחר
**Sign in with GitHub** (הכי קל — בלי סיסמה נוספת) ← אשר.

### 2. צור את השירות

1. בלוח של Render לחץ **New +** (למעלה) ← **Blueprint**.
2. בחר את המאגר `telegram-popup` (אם לא מופיע — לחץ
   **Configure account** ואשר גישה אליו).
3. לחץ **Apply** / **Deploy**.
4. המתן כ-5 דקות. כשהסטטוס נהיה ירוק (**Live**) — השרת באוויר.

### 3. קח את הכתובת והמפתח

- **הכתובת**: מופיעה למעלה בעמוד השירות —
  `https://telegram-popup-xxxx.onrender.com`. זה הדף החי שלך.
- **המפתח**: לשונית **Environment** ← שורת `TGPOPUP_KEY` ← לחץ על
  העין 👁 והעתק את הערך.

### 4. היכנס

פתח את הכתובת (מכל מכשיר — גם טלפון), הזן את המפתח פעם אחת, וזהו.
המפתח נשמר במכשיר; לא תתבקש שוב.

---

## שאלות שיעלו לך

**הדף אונליין לא נפתח מיד?** בתוכנית החינמית השרת נרדם אחרי רבע שעה
בלי מבקרים. הכניסה הראשונה מעירה אותו — לוקח עד דקה. רענן ותיכנס.

**הערוצים אונליין ריקים?** שרתי ענן נחסמים על ידי טלגרם ביתר קלות
ממחשב ביתי. פס המצב בראש הדף יגיד לך אם זה המצב, והתוכנה מנסה שוב
לבד. אם זה קבוע — זו מגבלה אמיתית של הרצה מהענן, והגרסה במחשב תמיד
תעבוד טוב יותר.

**איך משנים את רשימת הערוצים אונליין?** ב-Render ← **Environment** ←
ערוך את `TGPOPUP_CHANNELS` (שמות מופרדים בפסיק) ← **Save Changes**.
השרת יופעל מחדש לבד עם הרשימה החדשה.

**איך מעדכנים גרסה?** מעלים קבצים חדשים לגיטהאב (אותו תהליך כמו
חלק א' סעיף 3 — גרירה ו-Commit). Render מזהה את השינוי ובונה מחדש
לבד. עמוד ההורדה מתעדכן מיד.

**זה עולה כסף?** לא. גיטהאב חינם למאגרים ציבוריים, ו-Render חינם
בתוכנית הבסיסית (עם ההרדמה שהוזכרה).
